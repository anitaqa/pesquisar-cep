{
  "name": "pesquisar-cep",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "all:test": "npx cypress run",
    "test:sp:chrome" : "npx cypress run -b chrome --spec 'cypress/integration/*sp.spec.js'"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^9.1.1"
  }
}
